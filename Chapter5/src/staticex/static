자바는 클래스 외부에 어떤 변수도 둘 수 없다.
static이라는 키워드를 가진 변수는 여러 인스턴스가 그 값을 공유할 수 있다.
static변수는 heap 영역이 아니라 데이터 영역 메모리가 잡힌다
일반 멤버변수는 new를 통해 생성되지만
static 변수는 처음 프로그램이 로드될 때 데이터 영역에 생성된다
인스턴스 생성과 상관없이 사용가능하므로 클래스 이름으로 바로 참조해서 사용한다.
클래스 변수, 정적 변수라고도 함
