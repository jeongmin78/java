정보 은닉
 *public : 외부에 모두 오픈
 *private : 클래스에서만 사용
 *protected : 클래스간에 상속관계가 있을 때 상위클래스가 가진 private 변수를 public하게 사용하고 싶을 때 사용
 *아무것도 안 쓰는 경우(int, String 등 포함) : 같은 패키지 안에서는 사용 가능하지만 외부에서는 사용 불가

private를 사용하는 것이 좋지만 모든 것을 private을 사용할 필요는 없다.
 상황에 따라 사용.